# 验证报告：<<feature_name>>

> **文档定位**: SDDU 验证策略 — 指导 validate Agent 执行自主验证的场景和方法；验证结果见 validate-report.md  
> **前置依赖**: spec.md（需求规范）、review-report.md（审查报告，状态 passed）  
> **创建人**: <<创建人，如 SDDU Validate Agent>>  
> **创建时间**: <<日期，如 2026-06-20>>  
> **版本**: <<版本号，如 v1.0>>  
> **更新人**: <<修订人，如 SDDU Validate Agent>>  
> **更新时间**: <<日期，如 2026-06-20>>  
> **更新说明**: <<变更说明，如 初始创建>>

## 1. 验证概要
> 验证结果的量化总览

| 维度 | 实测数据 | 达标？ |
|------|---------|:--:|
| FR 测试覆盖 | <<fr_pct>>%（<<fr_covered>>/<<fr_total>>） | <<✅/❌>> |
| NFR 测试覆盖 | <<nfr_pct>>%（<<nfr_covered>>/<<nfr_total>>） | <<✅/❌>> |
| 构建 | 退出码 <<code>> | <<✅/❌>> |
| 接口一致性 | <<api_pass>>/<<api_total>> 通过 | <<✅/❌>> |
| 漂移项 | <<drift_count>> 项 | <<✅/⚠️/❌>> |
| 阻塞问题 | <<blocking_count>> 项 | <<✅/❌>> |

## 2. 自主验证场景（V1~VN）
> 验证 Agent 根据 spec/NFR/产物自主定义具体验证场景。以下为场景模板结构，
> 实际 V1~VN 条目由 Agent 在每次验证时自定义；每个 FR 至少对应 1 个 Vx，每个验证维度至少 1 条。

**验证对象来源**：
- `spec.md`：FR/NFR/EC → 逐项验证实现完整性和验收标准
- `build.md`：文件变更清单 → 覆盖完整性验证
- `src/` + `tests/`：源码和测试文件 → 测试覆盖和运行验证
- 构建产物：构建脚本、dist/ 输出 → 构建完整性验证

**Feature 类型自适应**（根据 Feature 特征调整验证维度权重）：
- **代码类 Feature**：全五维度验证（测试覆盖 + 接口数据 + 构建 + 性能边界 + 漂移检测）
- **模板/配置类 Feature**：侧重构建完整性 + 漂移检测 + 模板语法验证，可跳过接口数据
- **文档类 Feature**：侧重漂移检测 + 内容一致性，可跳过性能边界

| # | 验证对象 | 验证步骤 | 预期结果 | 验证维度 | 验证方法 |
|---|---------|---------|---------|---------|---------|
| V1 | <<验证对象，如：FR-021 登录功能>> | <<1. 运行测试；2. 调用 API>> | <<预期，如：测试通过，API 返回 200>> | <<维度，如：测试覆盖 + 接口数据>> | <<方法，如：npm test + curl>> |
| V2 | <<验证对象>> | <<步骤>> | <<预期结果>> | <<维度>> | <<方法>> |
| … | … | … | … | … | … |
| VN | <<验证对象>> | <<步骤>> | <<预期结果>> | <<维度>> | <<方法>> |

> **质量门槛（数量基线法）**：每个 FR ≥ 1 个 Vx，每个相关验证维度 ≥ 1 条 Vx，
> Vx 总数 = max(FR 数, 相关维度数 × 1) 起底。不满足则验证场景不合格，
> 需扩展条目至覆盖。

## 3. 测试覆盖验证
> 运行测试套件，统计覆盖率，逐项标注

### 3.1 功能需求 (FR) — 覆盖率 <<fr_pct>>%

| 需求 ID | spec 描述 | 测试结果 | 覆盖率 |
|---------|----------|:--:|:--:|
| FR-001 | <<描述>> | ✅ 通过 | 已覆盖 |
| FR-002 | <<描述>> | ❌ 失败 | 未覆盖 |

### 3.2 非功能需求 (NFR) — 覆盖率 <<nfr_pct>>%

| 需求 ID | spec 描述 | 测试结果 | 覆盖率 |
|---------|----------|:--:|:--:|
| NFR-001 | <<描述>> | ✅ 通过 | 已覆盖 |
| NFR-002 | <<描述>> | ❌ 无测试 | 未覆盖 |

## 4. 接口与数据实测
> 实际调用 API、检查数据库 schema，对比 spec 定义

| 检查项 | spec 要求 | 实测结果 | 一致？ |
|--------|----------|---------|:--:|
| <<API 端点 / 数据表>> | <<spec 描述>> | <<实测返回值/字段>> | <<✅/❌>> |
| <<API 端点 / 数据表>> | <<spec 描述>> | <<实测返回值/字段>> | <<✅/❌>> |

## 5. 构建与脚本验证
> 运行构建、lint、类型检查，确认可交付

| 检查项 | 命令 | 退出码 | 结果 |
|--------|------|:--:|:--:|
| 构建 | `node build-agents.cjs` | <<code>> | <<✅/❌>> |
| Lint | `npm run lint` | <<code>> | <<✅/❌>> |
| 类型检查 | `npx tsc --noEmit` | <<code>> | <<✅/❌>> |

## 6. 性能与边界验证
> 对 NFR 中的性能指标执行测量

| NFR | spec 要求 | 实测数据 | 达标？ |
|-----|----------|---------|:--:|
| <<性能指标>> | <<要求值>> | <<实测值>> | <<✅/❌>> |
| <<边界条件 EC>> | <<spec 要求>> | <<实测结果>> | <<✅/❌>> |

## 7. 漂移检测
> 扫描代码库，检测实现与规范的偏离

| 漂移类型 | 检测结果 |
|---------|---------|
| 孤立代码（有代码无需求） | <<文件列表 / ✅ 无>> |
| 需求缺失（有需求无代码） | <<FR 列表 / ✅ 无>> |
| 规格漂移（spec 被修改） | <<diff 结果 / ✅ 无>> |

## 8. 结论
> 验证最终结论，基于实测数据

**结论**: <<✅ 通过 / ⚠️ 有条件通过 / ❌ 不通过>>

| 指标 | 结果 |
|------|------|
| FR 覆盖率 | <<fr_pct>>% |
| NFR 覆盖率 | <<nfr_pct>>% |
| 构建 | <<✅/❌>> |
| 漂移 | <<drift_count>> 项 |
| 阻塞 | <<blocking_count>> 项 |

**理由**: <<简述>>

## 9. 修订记录
> 记录本文档的版本变更历史

| 版本 | 变更说明 | 日期 | 修订人 |
|------|---------|------|--------|
| <<版本号，如 v1.0>> | <<变更说明，如：初始创建>> | <<日期，如 2026-06-20>> | <<修订人，如 SDDU Validate Agent>> |
