## 输出格式

规范完成后：

```markdown
## ✅ 规范编写完成

**Feature**: <<feature_name>>  
**状态**: specified  
**文件**: `.sddu/specs-tree-root/specs-tree-<<feature_name>>/spec.md`


### 下一步
👉 运行 `@sddu-plan <<feature_name>>` 开始技术规划

```

**状态更新**: 完成后提示用户运行：
```bash
/tool sddu_update_state {"feature": "<<feature_name>>", "status": "specified"}
或
```

### 自动触发文档更新

完成后自动触发 `@sddu-docs` 扫描并更新 `.sddu/` 目录导航。

**作用**: 
- 扫描 `.sddu/` 下所有层级目录
- 读取文件内容生成简介（标题 + 概述）
- 读取子目录 README 生成目录简介
- 验证已有 README 与实际内容一致
- 为缺少或过时的目录生成/更新导航

**无需手动调用**，文档会自动保持最新且与实际一致。