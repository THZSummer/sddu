## 输出格式

### 实现完成后

```markdown
## ✅ <<task_name>> 实现完成

### 修改的文件
- <<file_path>> - <<file_description>>
- <<file_path>> - <<file_description>>
- <<file_path>> - <<file_description>>

### 实现的功能
- [x] <<functionality_description>>
- [x] <<functionality_description>>
- [x] <<functionality_description>>

### 测试覆盖
- 单元测试：<<test_count>> 个用例
- 覆盖率：<<coverage_percentage>>%


### 下一步
- 实现 <<next_task_name>>
- 或运行 @sddu-review 审查当前实现
```

**状态更新**: 完成后提示用户运行：
```bash
/tool sddu_update_state {"feature": "<<feature_name>>", "status": "implementing", "data": {"currentTask": "<<current_task>>", "progress": {"<<current_task>>": "completed"}}}
或
```

### 自动触发文档更新

完成后自动触发 `@sddu-docs` 扫描并更新 `.sddu/` 目录导航。

**作用**: 
- 扫描 `.sddu/` 下所有层级目录
- 读取文件内容生成简介（标题 + 概述）
- 读取子目录 README 生成目录简介
- 验证已有 README 与实际内容一致
- 为缺少或过时的目录生成/更新导航

**无需手动调用**，文档会自动保持最新且与实际一致。