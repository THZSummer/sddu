## 输出格式

规划完成后：

```markdown
## ✅ 技术规划完成

**Feature**: <<feature_name>>  
**阶段**: planned  
**状态**: tracked  
**文件**: `.sddu/specs-tree-root/specs-tree-<<feature_name>>/plan.md`

### 生成的 ADR
- <<adr_file_path>> - <<adr_title>>


### 下一步
👉 运行 `@sddu-tasks <<feature_name>>` 开始任务分解

```

## 完成报告

**状态更新**: 完成后 feature 的 phase 自动推进到 `planned`，status 保持 `tracked`。
```bash
/tool sddu_update_state {"feature": "<<feature_name>>", "phase": "planned"}
或
```

### 自动触发文档更新

完成后自动触发 `@sddu-tree` 扫描并更新 `.sddu/` 目录导航。

**作用**: 
- 扫描 `.sddu/` 下所有层级目录
- 读取文件内容生成简介（标题 + 概述）
- 读取子目录 README 生成目录简介
- 验证已有 README 与实际内容一致
- 为缺少或过时的目录生成/更新导航

**无需手动调用**，文档会自动保持最新且与实际一致。
