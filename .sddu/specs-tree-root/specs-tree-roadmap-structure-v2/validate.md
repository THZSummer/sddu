# 验证策略：specs-tree-roadmap-structure-v2

> **文档定位**: SDDU 验证策略 — 指导 validate Agent 执行自主验证的场景和方法；验证结果见 validate-report.md
> **前置依赖**: spec.md（12 FR / 8 NFR / 10 EC）、plan.md（§8 V-01~V-10）、tasks.md（TASK-006/007/008）、build.md、review-report.md（状态 ⚠️ 有条件通过，5 改进项）
> **创建人**: SDDU Validate Agent
> **创建时间**: 2026-09-24
> **版本**: v1.0
> **更新人**: SDDU Validate Agent
> **更新时间**: 2026-09-24
> **更新说明**: 初始创建 — 面向「模板/配置类 Feature」设计 V1~V6 六组验证场景；重点补齐 review 交接的沙箱演练（V5）与兼容旧结构推演（V4）

## 1. 验证概要
> 验证对象的量化总览

| 维度 | 数值 |
|------|:--:|
| 验证对象 | 2 个 `src/` 文件（1 输出模板全量重写 + 1 agent 模板定向改造） |
| 验证场景组 | 6（V1 构建 / V2 结构静态 / V3 Mermaid 静态 / V4 兼容旧结构 / V5 沙箱演练 / V6 边界） |
| 细化检查项 | 20 |
| 验证强度标注 | 实测 / 静态核查 / 推演 |
| 重点交接项 | review 改进 1（表格 entry 截断，C23）；plan V-05/V-06/V-09 沙箱场景 |

**Feature 类型判定**：模板/配置类 → 侧重构建完整性 + 模板语法/结构验证 + 沙箱行为验证；跳过接口数据与性能压测（无运行时 API）。

**基准文件**：
- `spec.md`：§5.1 FR-001~012、§6 NFR-001~008、§7 EC-001~010、§5.2 8 章 zone 分配、§5.5 表/entry 结构
- `plan.md`：§2.2 旧→新 zone 映射、§4.1~4.6、§8 V-01~V-10
- `build.md`：§2 变更清单、§3 验证证据、§4 TASK-008 交接说明
- `review-report.md`：C23 改进 1（表格截断）

## 2. 自主验证场景（V1~V6）

| # | 验证对象 | 验证步骤 | 预期结果 | 验证维度 | 验证方法 |
|---|---------|---------|---------|---------|---------|
| V1 | 构建与分发（NFR-003/006/007） | 1. `npm run build:agents`；2. `diff` src↔dist（output + agent 两处）；3. 连续两次构建递归 `md5sum`（`dist/templates/{output,agents}` 全 41 文件）；4. `diff -rq src/templates/outputs dist/templates/output`；5. 11 个 agent 模板逐个 `diff` | 退出码 0；dist 产物存在；两模板逐字节一致；二次构建 41 文件哈希不变；30 output 全一致；11 agent 全一致 | 构建完整性 + 确定性 | 实测：build + diff + 递归 md5sum |
| V2 | 结构静态（FR-001/002/006/007/008/010；NFR-004；EC-003） | 1. `grep -c '^## '`；2. 逐 zone 提取 `id`/`mode`；3. zone/entry 开闭计数；4. `grep -c '{{'` / `grep -Pc '\t'`；5. 三清单表头与分隔行列数；6. 元数据头字段计数；7. `grep -c '模式名'`（淘汰术语） | H2=8；zone-open 9 = 4 rewrite（含 meta）+ 5 preserve；zone-close 9；entry 5 组；`{{`=0；Tab=0；列数 6/5/6；元数据头 12 字段；淘汰术语 0 生效残留 | 模板语法 + 结构一致性 | 实测：grep/wc/awk |
| V3 | Mermaid 语法（FR-003；NFR-005；EC-007） | 1. `grep -c '```mermaid'`；2. `awk` 逐块提取；3. 逐块校验关键字 / 行数 / 空标签 / 占位符形式 / Tab / 方括号平衡 | 8 块；关键字与章节匹配；行数 ≤10；无空标签；占位符仅 `<<>>`；无 Tab；括号平衡 | 可渲染性（静态） | **静态核查**（本机无 `mmdc`，标注强度） |
| V4 | 兼容旧结构（FR-012；EC-001/002） | 按 agent §5.4.2 Step A 决策规则构造 3 类样例并推演：A 含 `next-actions` 旧 zone / B 无任何标记 / C 含新 zone | A → OLD_STRUCTURE + 迁移提示（不改文件）；B → NON_TEMPLATE + 三选项（不改文件）；C → INCREMENTAL 合并 | 行为正确性 + 非破坏性 | **推演**（可执行规程模拟，非真实 LLM Agent） |
| V5 | 沙箱演练（D5 决议；plan V-05/06/09；NFR-002/005；EC-009） | 1. `git show docs/roadmap-rewrite:.sddu/ROADMAP.md`（275 行）→ `/tmp/sddu-roadmap-v2-dryrun/`；2. 按**新模板**迁移生成 `ROADMAP-new.md`（12 版本 / 25 特性 / 风险 30 行分流 / 43 行动项 → P-xxx）；3. 自查 zone/entry 配对、H2=8、Mermaid=8、行数、`<<>>` 残留；4. 零丢失抽查（版本数/特性数/风险条数/行动条数 vs 承载位）；5. 工作区不变 | 275 行旧文档可完整映射；产物结构合法；行数 ≤450；无 `<<>>` 残留；四类内容均有承载位；工作区零改动 | 端到端映射 + 零丢失 | **推演 + 实测**（重放映射规程；渲染以 marked/remark-gfm 实测） |
| V6 | 工程边界（NFR-006；NG-001/005） | 1. `git status --short`；2. `git diff --name-only 46b5d8f HEAD -- src/`；3. 禁区 diff（`scripts` / `package.json` / `*.ts` / `*.cjs`）；4. `md5sum .sddu/ROADMAP.md` | 实现变更集恰 = 2 个 `src/` 文件；禁区 0 diff；`.sddu/ROADMAP.md` md5 不变；无越界 | 工程边界 + 漂移 | 实测：git status/diff/md5 |

## 3. 测试覆盖验证
> 模板类 Feature 无单元测试套件；以「结构/构建/行为」实测替代，逐项标注覆盖

### 3.1 功能需求 (FR) — 覆盖率 100%

| 需求 ID | 验证场景 | 覆盖率 |
|---------|----------|:--:|
| FR-001 | V2 | 已覆盖 |
| FR-002 | V2 | 已覆盖 |
| FR-003 | V2 + V3 | 已覆盖 |
| FR-004 | V2（§5/§6 风险字段）+ V5（分流） | 已覆盖 |
| FR-005 | V2（淘汰术语 0）+ V5 | 已覆盖 |
| FR-006 | V2（§4 六列 + entry 键） | 已覆盖 |
| FR-007 | V2（§3 类型列 + §6 entry 键） | 已覆盖 |
| FR-008 | V2（列数 6/5/6） | 已覆盖 |
| FR-009 | V2（三详情 entry 字段） | 已覆盖 |
| FR-010 | V2（12 字段 + H1 比对） | 已覆盖 |
| FR-011 | V2（agent §5/§5.4/§5.5/v3.2.0） | 已覆盖 |
| FR-012 | V4 + V5 | 已覆盖 |

### 3.2 非功能需求 (NFR) — 覆盖率 100%

| 需求 ID | 验证场景 | 覆盖率 |
|---------|----------|:--:|
| NFR-001 | V2（169 ≤ 170） | 已覆盖 |
| NFR-002 | V5（432 ≤ 450 抽样；576 全量） | 已覆盖（附容量发现） |
| NFR-003 | V1（递归幂等 + dist≡src） | 已覆盖 |
| NFR-004 | V2（Tab=0 + 风格比对） | 已覆盖 |
| NFR-005 | V3（静态核查） | 已覆盖（强度：静态） |
| NFR-006 | V1 + V6 | 已覆盖 |
| NFR-007 | V1（其余模板零改动） | 已覆盖 |
| NFR-008 | V2（判定表 8 行） | 已覆盖 |

### 3.3 边界情况 (EC) — 覆盖率 100%

| 需求 ID | 验证场景 | 覆盖率 |
|---------|----------|:--:|
| EC-001 / EC-002 | V4（B / A 样例） | 已覆盖 |
| EC-003 | V2（zone/entry 配对） | 已覆盖 |
| EC-004 / EC-005 | V2（agent §5.5 编码规则） | 已覆盖 |
| EC-006 | V2（特性清单推断标注） | 已覆盖 |
| EC-007 | V3 + V5 | 已覆盖 |
| EC-008 / EC-009 / EC-010 | V2（agent §5.5）+ V5 | 已覆盖 |

## 4. 接口与数据实测
> 本 Feature 无运行时 API / 数据库；产物为静态模板文件。以「构建分发链路（src→dist 裸拷贝）」作为等价接口验证，见 V1。**跳过**真实 API 调用。

## 5. 构建与脚本验证

| 检查项 | 命令 | 预期 |
|--------|------|:--:|
| 构建（agents） | `npm run build:agents` | 退出码 0；output 30 files |
| 类型检查 | 不适用（零运行时代码变更，NFR-006） | — |

## 6. 性能与边界验证
> 无性能指标；边界以 EC 场景与篇幅预算为主

| 边界 | 要求 | 验证 |
|------|------|:--:|
| EC-001/002 旧/非模板结构 | 不修改文件 + 提示 | V4 |
| EC-003 标记不成对 | 中止 + 诊断 | V2（配对校验） |
| NFR-001 模板篇幅 | ≤170 | V2（实测 169） |
| NFR-002 产物篇幅 | ≤450 | V5（抽样 432；全量 576 → 触发收敛提示） |

## 7. 漂移检测

| 漂移类型 | 检测方法 |
|---------|---------|
| 孤立代码（有代码无需求） | `git diff` 变更集 vs plan §5.1（V6） |
| 需求缺失（有需求无实现） | FR/NFR/EC 逐项 grep 锚点（V2/V3/V5） |
| 规格漂移 | 模板 / agent 集合一致性（章节 / zone / entry / 表列）比对（V2） |
| 文档基线漂移 | plan/tasks 计数字段 vs 实测（review-report C22 改进 2/3） |

## 8. 结论
> 验证结论判定标准

**判定规则**：
- ✅ 通过：全部场景通过，0 阻塞，FR/NFR/EC 覆盖 100%
- ⚠️ 有条件通过：核心场景通过，存在低优先非阻塞改进项
- ❌ 不通过：存在阻塞问题或核心 FR（P0）未通过

## 9. 修订记录
> 记录本文档的版本变更历史

| 版本 | 变更说明 | 日期 | 修订人 |
|------|---------|------|--------|
| v1.0 | 初始创建 — 定义 V1~V6 六组场景（20 检查项），重点设计 V5 沙箱演练（275 行旧 ROADMAP → 新结构）与 V4 兼容旧结构推演，补齐 review 改进 1 的可渲染性实测 | 2026-09-24 | SDDU Validate Agent |
