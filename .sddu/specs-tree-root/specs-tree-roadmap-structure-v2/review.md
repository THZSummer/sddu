# 审查策略：specs-tree-roadmap-structure-v2

> **文档定位**: SDDU 审查策略 — 定义本 Feature 的 C1~C24 审查清单与判定标准；审查结果见 review-report.md
> **前置依赖**: spec.md（12 FR / 8 NFR / 10 EC，v1.0）、plan.md（技术方案 V-01~V-10 + ADR-001/002/003）、tasks.md（9 任务）、build.md（构建产物）
> **创建人**: SDDU Review Agent
> **创建时间**: 2026-09-24
> **版本**: v1.0
> **更新人**: SDDU Review Agent
> **更新时间**: 2026-09-24
> **更新说明**: 初始创建 — 基于 spec v1.0 + plan v1.0 + 3 ADR + build.md，自主定义 C1~C24 审查清单（四维度）

## 1. 审查概要
> 审查结果的量化总览（此表为策略定义时的预估基线，最终数值以 review-report.md 为准）

| 维度 | 数值 |
|------|:--:|
| 审查文件数 | 3 个（1 输出模板全量重写 + 1 agent 模板定向改造 + build.md 记录） |
| 规范基准 | 12 FR / 8 NFR / 10 EC |
| 通过项 | — |
| 改进建议 | — |
| 阻塞问题 | — |

## 2. 自主审查清单（C1~C24）
> 审查 Agent 根据 spec/plan/build 产物自主定义具体审查项。

**审查对象来源**：
- `spec.md`：FR-001~012 / NFR-001~008 / EC-001~010 → 逐项核验实现完整性
- `plan.md`：§2.2 旧→新 zone 映射 + §4.1 8 章 zone 分配 + §4.2 Mermaid 落位 + §4.3 归属规则 + §4.4 表/entry 结构 + §4.6 迁移策略 + §8 V-01~V-10 + ADR-001/002/003 → 架构遵循性
- `build.md`：§1 任务状态 + §2 变更清单 + §3 验证证据 → 覆盖完整性与证据可信度
- `src/templates/outputs/sddu-roadmap.md.hbs`（169 行，全量重写）+ `src/templates/agents/sddu-roadmap.md.hbs`（468 行，定向改造）→ 模板 / 指令质量
- README「项目约束（Agent 必读）」→ 实现目标边界

**四维度指引**：
1. **规范符合性** — 对照 spec.md 逐 FR/NFR/EC 核验
2. **架构一致性** — 对照 plan.md §2.2/§4.x 与 ADR-001/002/003
3. **模板质量** — 标记自洽性、编写期说明隔离、可解析性、可渲染性
4. **工程边界** — 变更集范围与禁区（`.opencode/`、`.sddu/ROADMAP.md`、`scripts/`）

| # | 审查对象 | 审查基准 | 维度 | 审查方法 |
|---|---------|---------|------|---------|
| C1 | 8 H2 集合与顺序 == spec §5.2 表；「修订记录」恒为末位；命名遵循 D4（清单/详情） | FR-001 | 规范符合性 | `grep '^## '` 提取逐行对照 + 名称口径核对 |
| C2 | 8 content zone id / mode 与 FR-002 一一对应（3 rewrite + 5 preserve）；`meta` 不计入 8 | FR-002 | 规范符合性 | 逐 zone 提取 `id`/`mode` 与 spec §5.2 对照 |
| C3 | 8 章各含恰 1 个 ` ```mermaid ` 块；每图 ≤10 行；位于 zone 内顶部（rewrite 内 / preserve 首个 entry 前） | FR-003 | 规范符合性 | 围栏计数 + `awk` 提取块内行数 + 块/zone 相对位置核对 |
| C4 | 依赖风险三分归属规则存在且仅三主体承载；无独立「依赖与风险」章；项目级在 §4 / §7 有承载 | FR-004 | 规范符合性 | 模板 §4/§5/§6/§7 字段走查 + agent §5.5 判定表对照 + 旧章名负向 grep |
| C5 | 优先级 = P0/P1/P2 仅存 §6 entry；RICE/Top N/Phase/Wave/立即-短期-中期-远期在**生效语义区**零残留 | FR-005 | 规范符合性 | 正/负向 grep + 逐命中判定「淘汰声明 vs 残留」 |
| C6 | 问题登记制：P-xxx 编码 / 类型枚举 6 值 / 状态枚举 3 值 / 排序规则齐备；§4↔§7 键对应 | FR-006 | 规范符合性 | 模板 §4 表列 + entry 键核对 + agent §5.5 编码规则走查 |
| C7 | PR-xxx 进入 §3（类型=提案）与 §6（entry 键=PR-xxx） | FR-007 | 规范符合性 | 模板 §3 类型列 + §6 entry 键占位符核对 + agent 提案编码节走查 |
| C8 | 三张清单表列数与列名 == spec §5.5（6 / 5 / 6），顺序固定，无多余列 | FR-008 | 规范符合性 | 表头行与分隔行逐列切分比对 |
| C9 | 三个详情章 entry 键与字段集合 == spec §5.5；§7 描述为客观陈述、禁行动指令 | FR-009 | 规范符合性 | entry 块字段逐项核对 + §7 编写期说明语义核对 |
| C10 | 元数据头 12 字段与 H1 `# 版本 Roadmap：<<项目名称>>` 与基线逐项一致 | FR-010 | 规范符合性 | `grep '^> \*\*'` 字段计数/顺序 + `git show` 基线 H1 比对 |
| C11 | agent §5 章节定义表 / §5.4 zone 清单与合并语义 / §5.5 各章语义 + 归属判定表 / 修订记录 v3.2.0 | FR-011 | 规范符合性 | agent 定向逐节走查 + 与产物端集合一致性比对 |
| C12 | 旧 zone 识别 → 迁移提示（不静默重写）；全文无「自动迁移 / 静默重写」正面要求 | FR-012 | 规范符合性 | agent §5.4.1/§5.4.2/§8 走查 + 「自动迁移」命中逐条判定正负向 |
| C13 | 输出模板源文件 ≤170 行 | NFR-001 | 规范符合性 | `wc -l` |
| C14 | 生成的 ROADMAP 产物 ≤450 行（含 `/tmp` 沙箱演练实证） | NFR-002 | 规范符合性 | 沙箱产物 `wc -l`（全量 / 抽样两口径） |
| C15 | `npm run build:agents` 幂等且 dist ≡ src（output + agent 两处） | NFR-003 | 规范符合性 | 连续两次构建递归 md5 + `diff -rq` |
| C16 | 风格对齐 A 组（`<<变量名>>` 占位符 / 表格写法 / 无 Tab 缩进） | NFR-004 | 规范符合性 | 与 `sddu-plan.md.hbs` 头/表/占位符比对 + `grep -Pc '\t'` |
| C17 | Mermaid 语法合法：关键字正确 / 无空节点标签 / 占位符仅 `<<>>` 形式 / 无 Tab / 围栏配对 | NFR-005 | 规范符合性 | 逐块静态核查（mmdc 缺失，标注强度） |
| C18 | 零运行时代码：`git diff --name-only` 不含 `*.ts` / `scripts/**` / `package.json` | NFR-006 | 规范符合性 | 基线区间 diff 路径核对 |
| C19 | 除 2 个目标文件外 `src/templates/**` 其余 29 输出模板 + 10 Agent 指令零改动 | NFR-007 | 规范符合性 | `git diff --name-only` + 递归 diff 计数 |
| C20 | agent §5.5 含信息归属判定表；三条归属规则均可一次性判定、无歧义 | NFR-008 | 规范符合性 | §5.5 判定表逐行走查 + 与 spec §5.3 对照 |
| C21 | EC-001~010 逐项在模板 / agent 中有可落地的处置说明或锚点 | EC-001~010 | 规范符合性 | 逐 EC grep 锚点（模板字段 / agent §5.4·§5.5·§8） |
| C22 | plan §2.2 旧→新 zone 映射 / §4.1~4.6 / ADR-001·002·003 落地一致 | plan / ADR | 架构一致性 | 映射表逐行核对 + 模板 zone 走查 + agent Step A–D 走查 |
| C23 | 标记语法自洽（zone/entry 配对、仅 preserve 用 entry、编写期说明隔离、表格内 entry 的可渲染性） | plan §4.1 | 模板质量 | 配对计数 + preserve 区 entry 位置核查 + GFM 渲染器实测 |
| C24 | 工程边界：变更集 ⊆ 2 个 `src/` 文件 + 本 Feature 产物；`.sddu/ROADMAP.md` / `.opencode/` / `scripts/` 零 diff | NFR-006 / NG-001/005 | 工程边界 | `git status` / 基线区间 diff / `md5sum` |

> **质量门槛（数量基线法）**：12 FR + 8 NFR + 10 EC 全部落到 C1~C21（规范符合性，21 条）；plan/ADR 架构检查 C22（1 条）；模板质量 C23（1 条）；工程边界 C24（1 条）。四维度各 ≥1 条，Cx 总数 24 条，满足门槛。

## 3. 审查方法
> 静态审查（read-only）：文件走查 + `grep`/`diff`/`git`/渲染器实测；不修改任何实现文件，不执行 `git commit`/`push`，不执行破坏性构建。

## 4. 结论判据（依据 sddu-review Agent §6 审查标准）

| 条件 | 要求 | 结论映射 |
|------|------|---------|
| 阻塞问题 | 0 个 | 否则 ❌ 不通过 |
| 改进项 | < 5 个 | ≥5 且非阻塞 → ⚠️ 有条件通过 |
| 规范符合率 | 100% | 否则 ⚠️ 有条件通过 |

## 5. 审查详情
> 逐项执行结果、维度汇总、阻塞/改进清单与结论见 `review-report.md`（策略与报告分离）
