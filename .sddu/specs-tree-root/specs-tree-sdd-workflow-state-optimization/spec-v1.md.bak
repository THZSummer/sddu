# SDD 工作流状态优化规范

| 元数据 | 值 |
|--------|-----|
| **Feature ID** | FR-SDD-004 |
| **Feature 名称** | SDD 工作流状态优化 |
| **版本** | 1.0.9 |
| **优先级** | P1 |
| **创建日期** | 2026-04-01 |
| **作者** | SDD Team |
| **状态** | specified |
| **Phase** | 1 |

---

## 1. 问题陈述

### 1.1 背景
当前 SDD 工作流插件的状态管理采用**集中式 state.json 设计**，存在以下问题：

1. **单点故障风险**：所有 Feature 状态集中在 `.sdd/.specs/state.json` 单文件中，该文件损坏或丢失会影响全部 Feature
2. **并发冲突概率高**：多 Agent 同时更新不同 Feature 状态时需锁定同一文件，增加冲突概率和等待时间
3. **目录结构不自然**：状态文件与 Feature 目录分离，不符合"高内聚"设计原则，移动/复制 Feature 时需同步更新 state.json
4. **删除 Feature 时状态残留**：删除 Feature 目录后，state.json 中对应条目容易遗漏，导致脏数据累积
5. **状态转换不透明**：6 个阶段（spec → plan → tasks → build → review → validate）之间的转换规则不明确
6. **缺少状态验证**：无法验证状态转换的合法性，可能导致跳过必要阶段
7. **状态更新手动**：需要手动运行 `/tool sdd_update_state`，容易遗漏或出错
8. **缺少状态历史**：无法追溯 Feature 的状态变更历史
9. **依赖关系不清晰**：Feature 之间的依赖关系未与状态关联，可能导致依赖未就绪就开始开发

### 1.2 影响
- 开发流程不规范，容易跳过关键阶段
- 团队协作时状态不同步
- 无法准确评估项目进度
- 质量问题难以追溯

---

## 2. 目标范围

### 2.1 目标 (Goals)
- [G1] 采用分布式状态存储，支持多层级嵌套结构的状态聚合查询
  - 每个 specs 拥有独立的 `state.json`，位于各自 specs 目录下
  - 支持多层级嵌套：一级 specs → 二级 specs → 三级 specs
  - 聚合查询通过递归扫描所有层级的 `state.json` 实现
- [G2] 定义明确的状态转换规则和验证机制
- [G3] 实现状态自动更新，减少手动操作
- [G4] 记录状态变更历史，支持追溯审计
- [G5] 将依赖关系与状态关联，确保依赖就绪
- [G6] 提供状态查询和报告能力

**问题 - 目标映射**：

| 问题 | 对应目标 |
|------|----------|
| 单点故障风险 | G1 (分布式嵌套存储) |
| 并发冲突概率高 | G1 (分布式嵌套存储) |
| 目录结构不自然 | G1 (分布式嵌套存储) |
| 删除 Feature 时状态残留 | G1 (分布式嵌套存储) |
| 状态转换不透明 | G2 (转换规则) |
| 缺少状态验证 | G2 (验证机制) |
| 状态更新手动 | G3 (自动更新) |
| 缺少状态历史 | G4 (历史记录) |
| 依赖关系不清晰 | G5 (依赖关联) |

### 2.2 非目标 (Non-Goals)
- [NG1] 不改变现有的 6 阶段工作流模型
- [NG2] 不修改现有 Feature 的规范内容
- [NG3] 不涉及代码构建和测试执行（由后续阶段处理）
- [NG4] 不替代现有的版本控制系统
- [NG5] 不保留集中式 state.json 作为主要存储（仅迁移期兼容）

---

## 3. 用户需求

### 3.1 用户故事

| ID | 用户故事 | 优先级 |
|----|----------|--------|
| US-001 | 作为**开发者**，我想要**查看当前 Feature 的状态**，以便**了解工作进度** | P0 |
| US-002 | 作为**开发者**，我想要**自动更新状态**，以便**减少手动操作错误** | P0 |
| US-003 | 作为**Tech Lead**，我想要**查看所有 Feature 的状态概览**，以便**评估整体进度** | P0 |
| US-004 | 作为**开发者**，我想要**状态转换验证**，以便**确保遵循正确流程** | P1 |
| US-005 | 作为**审计员**，我想要**查看状态变更历史**，以便**追溯问题原因** | P1 |
| US-006 | 作为**开发者**，我想要**依赖状态检查**，以便**确认前置条件就绪** | P1 |
| US-007 | 作为**开发者**，我想要**状态异常告警**，以便**及时发现流程问题** | P2 |

---

## 4. 功能需求

### 4.1 状态定义 (FR-001)

**FR-001: 系统必须定义 6 个标准工作流状态**

| 状态 | 标识符 | 阶段 | 描述 |
|------|--------|------|------|
| 规范中 | `specified` | 1 | 规范文档已创建 |
| 规划中 | `planned` | 2 | 技术规划已完成 |
| 任务中 | `tasked` | 3 | 任务分解已完成 |
| 构建中 | `building` | 4 | 代码实现进行中 |
| 评审中 | `reviewed` | 5 | 代码评审已完成 |
| 已验证 | `validated` | 6 | 验证测试已通过 |

**验收标准**：
- [ ] AC-001.1: 状态定义在配置文件中集中管理
- [ ] AC-001.2: 每个状态有唯一的标识符和阶段编号
- [ ] AC-001.3: 状态定义支持扩展（未来可添加新状态）

### 4.2 状态转换 (FR-002)

**FR-002: 系统必须定义合法的状态转换规则**

```
初始 → specified → planned → tasked → building → reviewed → validated
         ↑_________________________________________|
                        (允许回退)
```

**合法转换矩阵**：

| 当前状态 | 允许转换到 |
|----------|------------|
| `initial` | `specified` |
| `specified` | `planned`, `initial` |
| `planned` | `tasked`, `specified` |
| `tasked` | `building`, `planned` |
| `building` | `reviewed`, `tasked` |
| `reviewed` | `validated`, `building` |
| `validated` | `reviewed` (终态，仅允许回退) |

**验收标准**：
- [ ] AC-002.1: 状态转换必须通过验证矩阵检查
- [ ] AC-002.2: 非法转换必须被拒绝并返回明确错误
- [ ] AC-002.3: 支持单步前进和单步回退
- [ ] AC-002.4: 不允许跳过中间状态

### 4.3 状态持久化 (FR-003)

**FR-003: 系统必须将状态信息持久化到每个 specs 目录下的 `state.json`**

**分布式设计原则**：
- **状态文件位置**：每个 specs 拥有独立的状态文件，位于各自 specs 目录下
- **嵌套结构**：支持多层嵌套，子 specs 目录位于父 specs 目录下
- **高内聚**：状态文件与 specs 文档（spec.md, plan.md, tasks.md）存放在同一目录
- **原子操作**：状态更新仅影响单个 specs 文件，降低并发冲突
- **聚合查询**：通过递归扫描所有层级的 `state.json` 实现跨 specs 状态汇总

**数据结构**（`[specs 目录]/state.json`）：
```json
{
  "feature": "specs-tree-feature-id",
  "name": "Feature 名称",
  "version": "1.0.0",
  "status": "specified",
  "phase": 1,
  "level": 1,
  "parent": null,
  "files": {
    "spec": "spec.md",
    "plan": "plan.md",
    "tasks": "tasks.md",
    "readme": "README.md"
  },
  "dependencies": {
    "on": ["dependent-feature-id"],
    "blocking": []
  },
  "history": [
    {
      "timestamp": "2026-04-01T10:00:00Z",
      "from": "initial",
      "to": "specified",
      "triggeredBy": "@sdd-spec",
      "comment": "规范完成"
    }
  ],
  "createdAt": "2026-04-01T00:00:00Z",
  "updatedAt": "2026-04-01T00:00:00Z"
}
```

**目录结构**：

```
.sdd/
├── specs-tree-root/                    # specs 根目录
│   ├── spec.md                         # 规格说明文档
│   ├── plan.md                         # 技术计划文档
│   ├── tasks.md                        # 任务分解文档
│   ├── state.json                      # 状态跟踪文件
│   ├── specs-tree-feature-a/           # 一级 specs
│   │   ├── spec.md                     # 规格说明文档
│   │   ├── plan.md                     # 技术计划文档
│   │   ├── tasks.md                    # 任务分解文档
│   │   ├── state.json                  # 状态跟踪文件
│   │   └── specs-tree-sub/             # 二级 specs（嵌套）
│   │       ├── spec.md                 # 规格说明文档
│   │       ├── plan.md                 # 技术计划文档
│   │       ├── tasks.md                # 任务分解文档
│   │       └── state.json              # 状态跟踪文件
│   ├── specs-tree-feature-b/           # 一级 specs
│   │   ├── spec.md                     # 规格说明文档
│   │   ├── plan.md                     # 技术计划文档
│   │   ├── tasks.md                    # 任务分解文档
│   │   └── state.json                  # 状态跟踪文件
│   └── specs-tree-multi-module/        # 一级 specs（已有实例）
│       ├── spec.md                     # 规格说明文档
│       ├── plan.md                     # 技术计划文档
│       ├── tasks.md                    # 任务分解文档
│       └── state.json                  # 状态跟踪文件
```

**说明**：
- `.sdd/specs-tree-root/` 是 specs 根目录
- 根目录必须包含 `spec.md`, `plan.md`, `tasks.md`, `state.json`
- 一级 specs：`.sdd/specs-tree-root/specs-tree-[feature]/`
- 二级 specs：`.sdd/specs-tree-root/specs-tree-[feature]/specs-tree-[sub]/`
- 所有 specs 目录必须以 `specs-tree-` 前缀开头
- 每个 specs 目录必须包含 `spec.md`, `plan.md`, `tasks.md`, `state.json`
- 支持多层嵌套结构

**验收标准**：
- [ ] AC-003.1: `state.json` 必须位于每个 specs 目录而非集中式存储
- [ ] AC-003.2: 状态更新必须针对单个 specs 文件进行原子操作
- [ ] AC-003.3: `updatedAt` 字段必须在每次状态变更时更新
- [ ] AC-003.4: 支持 JSON 格式验证
- [ ] AC-003.5: 新建 specs 时自动创建初始 state.json
- [ ] AC-003.6: 支持多层级嵌套结构（一级 → 二级 → 三级）
- [ ] AC-003.7: 支持跨 specs 的状态聚合查询（通过递归扫描实现）
- [ ] AC-003.8: 嵌套层级深度限制为 10 层，超过时返回警告
- [ ] AC-003.9: 递归扫描时自动跳过符号链接避免无限循环

### 4.4 状态自动更新 (FR-004)

**FR-004: 系统必须在工作流阶段完成时自动更新状态**

**触发条件**：

| 触发事件 | 状态变更 |
|----------|----------|
| `@sdd-spec` 完成 | `initial` → `specified` |
| `@sdd-plan` 完成 | `specified` → `planned` |
| `@sdd-tasks` 完成 | `planned` → `tasked` |
| `@sdd-build` 完成 | `tasked` → `building` |
| `@sdd-review` 完成 | `building` → `reviewed` |
| `@sdd-validate` 完成 | `reviewed` → `validated` |

**验收标准**：
- [ ] AC-004.1: 各阶段 Agent 完成后自动触发状态更新
- [ ] AC-004.2: 支持手动状态更新作为备用
- [ ] AC-004.3: 状态更新失败时必须有明确错误提示

### 4.5 状态历史 (FR-005)

**FR-005: 系统必须记录状态变更历史**

**历史记录结构**：
```json
{
  "history": [
    {
      "timestamp": "2026-04-01T10:00:00Z",
      "from": "specified",
      "to": "planned",
      "triggeredBy": "@sdd-plan",
      "actor": "agent-name",
      "comment": "技术规划完成"
    }
  ]
}
```

**验收标准**：
- [ ] AC-005.1: 每次状态变更必须记录历史
- [ ] AC-005.2: 历史记录必须包含时间戳和触发源
- [ ] AC-005.3: 支持查询 Feature 的完整状态历史
- [ ] AC-005.4: 历史记录不可篡改（只追加）

### 4.6 依赖状态检查 (FR-006)

**FR-006: 系统必须在状态变更前检查依赖状态**

**依赖规则**：
- 如果 specs A 依赖 specs B，则 B 的状态必须 ≥ A 的目标状态
- 依赖检查在状态前进时强制执行
- 依赖检查在状态回退时警告提示

**验收标准**：
- [ ] AC-006.1: 状态前进前必须验证所有依赖就绪
- [ ] AC-006.2: 依赖未就绪时阻止状态变更并提示
- [ ] AC-006.3: 支持循环依赖检测

### 4.7 状态查询 (FR-007)

**FR-007: 系统必须提供状态查询接口**

**查询能力**：
- 按 specs ID 查询单个状态
- 按状态过滤查询
- 按阶段过滤查询
- 查询所有 specs 状态概览

**验收标准**：
- [ ] AC-007.1: 支持通过工具命令查询状态
- [ ] AC-007.2: 支持通过 API 查询状态（如适用）
- [ ] AC-007.3: 查询响应时间 < 100ms

### 4.8 状态报告 (FR-008)

**FR-008: 系统必须生成状态报告**

**报告内容**：
- 各状态 specs 数量统计
- 各阶段进度百分比
- 阻塞 specs 列表（依赖未就绪）
- 长期停滞 specs 告警（>7 天无状态变更）

**验收标准**：
- [ ] AC-008.1: 支持生成文本格式报告
- [ ] AC-008.2: 支持生成可视化图表（如适用）
- [ ] AC-008.3: 报告可导出为 Markdown

### 4.9 跨 specs 聚合查询 (FR-009)

**FR-009: 系统必须支持跨 specs 的状态聚合查询**

**聚合查询机制**：
- **递归扫描**：通过递归扫描所有层级的 `state.json` 实现
- **多层级支持**：支持一级、二级、三级...嵌套 specs 的状态聚合
- **深度限制**：递归深度限制为 10 层，防止栈溢出
- **循环检测**：扫描时检测并跳过符号链接避免无限循环
- **统计聚合**：计算各状态/阶段的 specs 数量分布
- **依赖图构建**：根据 `dependencies.on` 字段构建 specs 依赖关系图
- **阻塞检测**：识别因依赖未就绪而无法推进的 specs

**查询能力**：
| 查询类型 | 说明 | 示例 |
|----------|------|------|
| 状态分布 | 统计各状态的 specs 数量 | `{specified: 5, planned: 3, validated: 2}` |
| 阶段进度 | 计算各阶段的完成百分比 | `Phase 1: 100%, Phase 2: 60%, Phase 3: 30%` |
| 层级分布 | 统计各层级 specs 数量 | `{level1: 5, level2: 3, level3: 1}` |
| 依赖链 | 查询某 specs 的完整依赖链 | `A ← B ← C` |
| 阻塞列表 | 列出所有因依赖阻塞的 specs | `[specs-tree-x, specs-tree-y]` |

**数据结构示例**（聚合查询结果）：
```json
{
  "summary": {
    "totalSpecs": 10,
    "byStatus": {
      "specified": 2,
      "planned": 3,
      "tasked": 2,
      "building": 2,
      "reviewed": 0,
      "validated": 1
    },
    "byPhase": {
      "1": 2,
      "2": 3,
      "3": 2,
      "4": 2,
      "5": 0,
      "6": 1
    },
    "byLevel": {
      "1": 5,
      "2": 3,
      "3": 2
    }
  },
  "progress": {
    "overallPercentage": 45,
    "phaseProgress": [
      {"phase": 1, "percentage": 100},
      {"phase": 2, "percentage": 60},
      {"phase": 3, "percentage": 30}
    ]
  },
  "blocked": [
    {
      "feature": "specs-tree-feature-x",
      "status": "planned",
      "blockedBy": ["specs-tree-feature-y"],
      "reason": "依赖 specs-tree-feature-y 未达到 planned 状态"
    }
  ]
}
```

**验收标准**：
- [ ] AC-009.1: 支持查询所有 specs 的状态分布
- [ ] AC-009.2: 支持查询整体进度百分比
- [ ] AC-009.3: 支持查询阻塞 specs 列表及原因
- [ ] AC-009.4: 聚合查询响应时间 < 500ms（100 个 specs 以内）
- [ ] AC-009.5: 支持多层级嵌套 specs 的递归扫描
- [ ] AC-009.6: 支持按层级过滤查询结果（如仅查询一级 specs）
- [ ] AC-009.7: 递归扫描时自动跳过符号链接
- [ ] AC-009.8: 嵌套深度超过 10 层时返回警告但不中断查询

---

## 5. 非功能需求

### 5.1 性能 (NFR-001)
- **NFR-001**: 状态查询响应时间必须 < 100ms
- **NFR-002**: 状态更新操作必须在 < 500ms 内完成
- **NFR-003**: 支持同时管理 ≥ 100 个 specs

### 5.2 可靠性 (NFR-004)
- **NFR-004**: 状态数据必须持久化，系统重启后不丢失
- **NFR-005**: 状态更新失败时必须回滚，保持数据一致性
- **NFR-006**: 并发状态更新必须有锁机制防止冲突

### 5.3 可维护性 (NFR-007)
- **NFR-007**: 状态定义必须与代码分离，支持配置化
- **NFR-008**: 状态转换规则必须可配置，无需修改代码
- **NFR-009**: 必须提供状态迁移工具（如需变更状态模型）

### 5.4 可用性 (NFR-010)
- **NFR-010**: 状态变更必须有明确的确认提示
- **NFR-011**: 错误消息必须清晰说明原因和解决方案
- **NFR-012**: 必须提供状态操作的回滚能力

### 5.5 兼容性 (NFR-013)
- **NFR-013**: 必须兼容现有 `state.json` 格式（支持从旧格式迁移）
- **NFR-014**: 必须支持向后兼容的状态查询接口
- **NFR-015**: 必须支持分布式状态与集中式状态的互操作

---

## 6. 约束条件

### 6.1 技术约束
- **C-001**: 必须使用 JSON 格式存储状态数据
- **C-002**: 必须与现有 SDD 工具链集成
- **C-003**: 不能引入外部数据库依赖（保持轻量级）
- **C-004**: 必须遵循 TREE.md 目录结构规范
- **C-005**: 所有 specs 目录必须使用 `specs-tree-` 前缀命名
- **C-006**: 必须支持从旧版集中式 state.json 格式平滑迁移到分布式格式

### 6.2 流程约束
- **C-007**: 必须保持 6 阶段工作流模型不变
- **C-008**: 状态变更必须由授权 Agent 触发
- **C-009**: 迁移期间需支持 specs 目录的自动发现和递归扫描

### 6.3 时间约束
- **C-010**: 必须在 Phase 1 完成规范，Phase 2 完成实现

---

## 7. 依赖关系

### 7.1 内部依赖
| 依赖 Feature | 依赖类型 | 说明 |
|--------------|----------|------|
| `specs-tree-sdd-plugin-baseline` | 强依赖 | 基础 SDD 插件功能 |
| `specs-tree-sdd-plugin-phase2` | 强依赖 | Phase 2 工作流支持 |

### 7.2 外部依赖
| 依赖 | 类型 | 说明 |
|------|------|------|
| Node.js | 运行时 | ≥ 18.0.0 |
| VS Code API | 运行时 | 最新稳定版 |

### 7.3 被阻塞的 Feature
| Feature | 说明 |
|---------|------|
| `specs-tree-sdd-multi-module` | 依赖本优化后的状态管理 |

---

## 8. 边界情况

### 8.1 错误处理 (EC-001)
- **EC-001**: 迁移现有 state.json 到分布式结构时，自动为每个 specs 创建独立的 `state.json`
- **EC-002**: specs 目录不存在 `state.json` 时，自动创建初始状态文件（状态为 `initial`）
- **EC-003**: `state.json` 格式错误时，返回解析错误并提示修复
- **EC-004**: specs 目录不存在时，拒绝状态更新并提示创建 specs

### 8.2 并发处理 (EC-005)
- **EC-005**: 并发状态更新时，使用文件锁防止数据损坏（针对单个 specs 文件）
- **EC-006**: 锁获取超时（>5s）时，返回繁忙错误并建议重试

### 8.3 极端情况 (EC-007)
- **EC-007**: specs 数量超过 1000 时，聚合查询性能降级告警
- **EC-008**: 历史记录超过 10000 条时，自动归档旧记录
- **EC-009**: 聚合查询超时（>2s）时，返回部分结果并提示重试

### 8.4 依赖场景 (EC-010)
- **EC-010**: 检测到循环依赖（A 依赖 B，B 依赖 A）时，阻止状态前进并报错
- **EC-011**: specs 目录被删除时，自动从聚合查询结果中移除
- **EC-012**: specs 被移动或重命名时，自动更新依赖引用

### 8.5 多层嵌套场景 (EC-013)
- **EC-013**: 嵌套层级超过 10 层时，返回警告并建议扁平化结构
- **EC-014**: 子 specs 状态变更时，支持通知父 specs（可选配置）
- **EC-015**: 递归扫描时遇到符号链接，跳过避免无限循环
- **EC-016**: 检测到循环嵌套（A 是 B 的父目录，B 又是 A 的父目录）时，报错并拒绝操作
- **EC-017**: 递归扫描深度达到限制时，返回已扫描结果并提示部分数据可能缺失
- **EC-018**: specs 目录命名不符合规范时，扫描时跳过并记录警告日志

---

## 9. 开放问题

| ID | 问题 | 负责人 | 状态 |
|----|------|--------|------|
| Q-001 | 是否需要支持自定义状态？ | SDD Team | 待讨论 |
| Q-002 | 状态历史存储是否单独文件？ | SDD Team | 待决定 |
| Q-003 | 是否需要状态变更通知机制（如邮件/消息）？ | SDD Team | 待调研 |
| Q-004 | 是否支持与外部项目管理工具（如 Jira）同步？ | SDD Team | 后续考虑 |
| Q-005 | 嵌套层级的最佳实践深度是多少？ | SDD Team | 待讨论 |

---

## 10. 附录

### 10.1 术语表
| 术语 | 定义 |
|------|------|
| specs | SDD 工作流中的一个功能模块或子模块 |
| Phase | 工作流的 6 个阶段之一 |
| Status | specs 的当前状态标识 |
| Agent | 执行特定阶段任务的自动化助手 |
| 分布式状态 | 每个 specs 拥有独立的 state.json，位于各自目录下 |
| 状态聚合查询 | 通过递归扫描所有 specs 的 state.json 实现跨 specs 状态汇总 |
| 依赖链 | specs 之间通过 `dependencies.on` 字段建立的依赖关系 |
| 嵌套层级 | specs 目录的层级深度（一级、二级、三级...） |
| 循环嵌套 | 目录结构中出现的循环引用（A 是 B 的父目录，B 又是 A 的父目录） |
| 递归深度限制 | 聚合查询时递归扫描的最大层级数（默认 10 层） |
| specs-tree-root | `.sdd/` 下的 specs 根目录 |

### 10.2 参考文档
- [SDD 工作流规范](../README.md)
- [SDD 目录结构规范](../../TREE.md)
- [state.json Schema](./state-schema.json) (待创建)
- [分布式状态迁移指南](./migration-guide.md) (待创建)

---

## 11. 文档历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| 1.0.9 | 2026-04-01 | SDD Team | **TREE.md 权威定义对齐修正**：全面修正目录结构描述以符合 TREE.md 规范<br>• 更新根目录路径：`.sdd/.specs/` → `.sdd/specs-tree-root/`<br>• 更新目录命名：`sdd-feature-` → `specs-tree-feature-`，`sdd-multi-module/` → `specs-tree-multi-module/`<br>• 更新二级目录命名：`[sub-feature]/` → `specs-tree-[sub]/`<br>• 更新根目录文档完整性：添加 `spec.md, plan.md, tasks.md, state.json`<br>• 更新每个 specs 文档完整性：从仅 `spec.md, state.json` 改为必须包含 `spec.md, plan.md, tasks.md, state.json`<br>• 更新目录结构示例：完整展示所有必需文档<br>• 更新说明文字：全部符合 TREE.md 描述 |
| 1.0.7 | 2026-04-01 | SDD Team | **TREE.md 多层嵌套结构修正**：修正结构描述以符合 TREE.md 多层嵌套定义<br>• 更新 G1 目标：强调"状态聚合查询"能力，简化路径描述<br>• 更新问题 - 目标映射：从"分布式存储"改为"分布式嵌套存储"<br>• 更新 FR-003：简化路径描述，删除强制 `specs-tree-` 前缀要求，新增 AC-003.8/AC-003.9（深度限制、符号链接处理）<br>• 更新目录结构示例：调整缩进格式，删除"必须使用 specs-tree-前缀"描述<br>• 更新 FR-009：添加深度限制、循环检测机制，新增 AC-009.6~AC-009.8<br>• 扩展 EC-016~EC-018：新增循环嵌套检测、深度限制处理、命名规范警告<br>• 保留当前目录命名约定（后续单独起 feature 归一化） |
| 1.0.6 | 2026-04-01 | SDD Team | **TREE.md 一致性修正**：全面修正以符合 TREE.md 目录结构规范<br>• 更新根目录路径：`.sdd/.specs/` → `.sdd/specs-tree-root/`<br>• 更新目录命名：`feature-name/` → `specs-tree-feature-name/`<br>• 更新嵌套规则：从"平铺无嵌套"改为"支持多层嵌套"<br>• 更新 G1 目标：支持多层级嵌套结构<br>• 更新 FR-003：目录结构示例、说明文字、验收标准全部更新<br>• 更新 FR-009：聚合查询支持多层级递归扫描<br>• 新增 EC-013~EC-016：多层嵌套场景边界情况<br>• 更新术语表：统一使用 specs-tree 相关术语<br>• 更新依赖关系：添加 specs-tree- 前缀 |
| 1.0.5 | 2026-04-01 | SDD Team | **重复章节修复**：删除第 4.9 节重复的 FR-008 状态报告章节，修正后续章节编号（4.10 → 4.9） |
| 1.0.4 | 2026-04-01 | SDD Team | **目录结构修正**：根据 TREE.md 修正为平铺结构<br>• 更新 G1 目标：从"层级化状态继承"改为"平铺式状态存储 + 跨 Feature 聚合查询"<br>• 更新 FR-003：删除父子层级描述，改为平铺结构示例，删除 `aggregation` 字段<br>• 删除 FR-010：平铺结构不需要层级状态传播<br>• 新增 FR-009：跨 Feature 聚合查询（通过平铺扫描实现）<br>• 更新 EC-010~EC-012：从层级场景改为依赖场景<br>• 更新术语表：删除层级化术语，改为平铺式术语<br>• 更新问题 - 目标映射：删除层级相关映射 |
| 1.0.3 | 2026-04-01 | SDD Team | **目录结构修正**：修正目录结构示例，明确 `.sdd/.specs/` 是根容器而非父 Feature<br>• 更新 FR-003 目录结构示例：添加 `single-feature` 顶层 Feature 示例，明确父子层级关系<br>• 添加目录结构说明：解释根容器、顶层 Feature、子 Feature、父 Feature 的定义<br>• 更新 FR-010 示例：添加父状态聚合计算说明，明确 `computedStatus` 字段 |
| 1.0.2 | 2026-04-01 | SDD Team | **架构重构**：状态管理升级为层级化状态继承设计<br>• 更新 G1 目标：从"分布式存储 + 聚合查询"改为"层级化状态继承"<br>• 新增 FR-010：层级状态传播功能（子→父自动聚合）<br>• 删除 FR-009：状态聚合查询不再作为独立功能，改为内置能力<br>• 重构 FR-003：添加层级化设计原则和 aggregation 字段<br>• 更新目录结构：展示父子 Feature 层级示例<br>• 更新 EC-010~EC-012：新增层级场景边界情况<br>• 更新术语表：统一使用"层级化"、"聚合"、"继承"术语 |
| 1.0.1 | 2026-04-01 | SDD Team | **规范修订**：修复第 1 章与第 2 章的术语冲突<br>• 重写 G1 目标：从"统一的状态管理中心"改为"分布式状态存储 + 聚合视图"<br>• 新增问题 - 目标映射表，明确 9 个问题与 6 个目标的对应关系<br>• 新增 NG5：明确不保留集中式 state.json 作为主要存储<br>• 确保全文术语一致性（分布式存储 + 聚合查询） |
| 1.0.0 | 2026-04-01 | SDD Team | 初始版本 |
| 1.0.0 | 2026-04-01 | SDD Team | **规范更新**：状态管理从集中式改为分布式目录层级设计<br>• 更新 1.1 问题陈述，强调集中式 state.json 的单点故障、并发冲突等问题<br>• 重构 FR-003：状态持久化改为每个 Feature 独立 state.json<br>• 新增 FR-009：跨 Feature 状态聚合查询能力<br>• 更新 AC-003.x 验收标准，匹配分布式设计<br>• 新增 C-007、C-008 约束，保持迁移期兼容性<br>• 扩展 EC-001~EC-011 边界情况，覆盖迁移场景 |
