# 验证策略：specs-tree-roadmap-output-template

> **文档定位**: SDDU 验证策略 — 指导 validate Agent 执行自主验证的场景和方法；验证结果见 validate-report.md  
> **前置依赖**: spec.md（需求规范 12 FR / 8 NFR / 10 EC + SUP-001）、plan.md（§8 V-01~V-12）、build.md、review-report.md（状态 passed，3 改进项）  
> **创建人**: SDDU Validate Agent  
> **创建时间**: 2026-09-22  
> **版本**: v1.0  
> **更新人**: SDDU Validate Agent  
> **更新时间**: 2026-09-22  
> **更新说明**: 初始创建 — 面向「模板/配置类 Feature」设计 V1~V5 五组验证场景，重点补齐 review C20 交接的 FR-005 增量合并机制动态验证

## 1. 验证概要
> 验证对象的量化总览

| 维度 | 数值 |
|------|:--:|
| 验证对象 | 2 个 `src/` 文件（1 NEW 输出模板 + 1 MODIFY agent 模板） |
| 验证场景组 | 5（V1 构建 / V2 模板结构 / V3 增量合并动态 / V4 工程边界 / V5 覆盖度） |
| 细化检查项 | 21 |
| 验证强度标注 | 实测 / 推演（可执行规程模拟） |
| 重点交接项 | review C20（FR-005 动态验证）；C23、C26（低优先文档项） |

**Feature 类型判定**：模板/配置类 → 侧重构建完整性 + 模板语法/结构验证 + 漂移检测；跳过接口数据与性能压测（无运行时 API）。

**基准文件**：
- `spec.md`：5.2 功能需求表（FR-001~012）、§6 NFR-001~008、§7 EC-001~010、§5.1 SUP-001
- `plan.md`：§4.1.1 标记语法、§4.1.2 合并算法、§8 验证场景 V-01~V-12
- `build.md`：§2 变更清单、§3 验证证据、§4 TASK-007 静态校验说明
- `review-report.md`：C20/C23/C26 三改进项

## 2. 自主验证场景（V1~VN）

| # | 验证对象 | 验证步骤 | 预期结果 | 验证维度 | 验证方法 |
|---|---------|---------|---------|---------|---------|
| V1 | 构建与分发（FR-001 / FR-011 / NFR-002 / NFR-008） | 1. `npm run build:agents`；2. `npm run build`；3. `ls dist/templates/output/sddu-roadmap.md.hbs`；4. `diff` src↔dist；5. 连续两次构建比对 30 个 dist `.hbs` md5；6. `diff -rq src/templates/outputs dist/templates/output` | 构建退出码 0；dist 产物存在（单数 `output/`）；逐字节一致；二次构建哈希不变；30 个模板全部一致 | 构建完整性 + 确定性 | 实测：build + diff + md5sum |
| V2 | 模板结构（FR-002/003/004/005/006、NFR-001/006/007） | 1. grep 计数 zone 开/闭、rewrite/preserve、entry、H2、行数、变量数、`{{`、Tab；2. 提取 7 个 H2 顺序；3. 提取元数据头字段；4. 最小样例渲染（`<<…>>`→SAMPLE）后校验表格列数一致 + 标记成对 | zone 16（8 开 + 8 闭）、rewrite 5、preserve 3、entry 2、H2 7、行数 ≤200、`{{`=0、Tab=0；H2 集合/顺序 == spec FR-003 表；元数据头 = 统一 8 + 专属 3；渲染产物表格列数一致、标记成对 | 模板语法 + 结构一致性 | 实测：grep/wc + 最小样例渲染 + Python 配对校验 |
| V3 | 增量合并机制（FR-005 核心 / OP-002；EC-004/EC-007；review C20） | 在 `/tmp/sddu-validate-roadmap/` 构造含 8 zone + 3 entry 的最小 ROADMAP 产物，按 agent §5.4 Step A–D 编写**可执行规程模拟**脚本，执行：**V-06** 输入未变二次合并 → 全文件 diff；**V-07** 改 rewrite 区 + 新增 1 entry → 校验 rewrite 整块替换 / 既有 entry 逐字保留 / 新 entry 追加；**V-08** 无标记文件 → 判定非模板结构 + 文件零改动；**V-09** 标记不成对 → 中止 + 含行号诊断 | V-06 输入未变时逐字节一致、revision-log 不追加；V-07 既有 3 entry 逐字保留 + 新 entry 追加（计数 4）+ 顺序不变 + rewrite 整块替换；V-08 返回「非模板结构」且文件 md5 不变、无输出文件；V-09 中止且文件不变、诊断含行号 | 行为正确性 + 幂等 + 非破坏性 | 推演（可执行规程模拟，非真实 LLM Agent 运行）+ 沙箱文件状态对比 |
| V4 | 工程边界（NFR-003 / NFR-008 / NG-004/005；plan V-11/V-12） | 1. `git branch --show-current`；2. `git status --short`；3. `git diff --stat`；4. 核对禁区路径 `.opencode/` / `.sddu/ROADMAP.md` | 分支 == `feature/roadmap-output-template`；实现变更仅 2 个 `src/` 文件（1 M + 1 NEW）；`scripts/`/`*.ts`/`package.json`/`.opencode/`/`.sddu/ROADMAP.md`/旧 Feature spec 零 diff | 工程边界 + 漂移 | 实测：git status/diff |
| V5 | 覆盖度（12 FR / 8 NFR / 10 EC + SUP-001） | 逐项对照 grep/静态证据抽查关键项：FR-006 排除章节计数、FR-007 §6 归一化 diff、FR-008/009/010/012 字样计数、EC-001~010 锚点、SUP-001「内置固定格式」=0、NFR-008 其他模板零改动 | FR 12/12、NFR 8/8、EC 10/10 落实；SUP-001 一致性成立（「内置固定格式」表述为 0） | 需求覆盖 + 规范一致性 | 实测：grep 计数 + 归一化 diff |

## 3. 测试覆盖验证
> 模板类 Feature 无单元测试套件；以「结构/构建/行为」实测替代，逐项标注覆盖

### 3.1 功能需求 (FR) — 覆盖率 100%

| 需求 ID | 验证场景 | 覆盖率 |
|---------|----------|:--:|
| FR-001 | V1 | 已覆盖 |
| FR-002 | V2 | 已覆盖 |
| FR-003 | V2 | 已覆盖 |
| FR-004 | V2 | 已覆盖 |
| FR-005 | V2（静态）+ V3（动态） | 已覆盖 |
| FR-006 | V2 + V5 | 已覆盖 |
| FR-007 | V5（§6 归一化 diff） | 已覆盖 |
| FR-008 | V5（骨架字样计数） | 已覆盖 |
| FR-009 | V5（笔误字样计数） | 已覆盖 |
| FR-010 | V5（判定标准文字） | 已覆盖 |
| FR-011 | V1 | 已覆盖 |
| FR-012 | V3（V-08）+ V5 | 已覆盖 |

### 3.2 非功能需求 (NFR) — 覆盖率 100%

| 需求 ID | 验证场景 | 覆盖率 |
|---------|----------|:--:|
| NFR-001 | V2（Tab/风格） | 已覆盖 |
| NFR-002 | V1（二次构建哈希） | 已覆盖 |
| NFR-003 | V4 | 已覆盖 |
| NFR-004 | V5（§6 两级路径） | 已覆盖 |
| NFR-005 | V5（EC-001/002 文案） | 已覆盖 |
| NFR-006 | V2（标题稳定）+ V3 | 已覆盖 |
| NFR-007 | V2（行数/`{{`/脚本） | 已覆盖 |
| NFR-008 | V1 + V4 | 已覆盖 |

## 4. 接口与数据实测
> 本 Feature 无运行时 API / 数据库；产物为静态模板文件。以「构建分发链路（src→dist 裸拷贝）」作为等价接口验证，见 V1。**跳过**真实 API 调用。

## 5. 构建与脚本验证

| 检查项 | 命令 | 预期 |
|--------|------|:--:|
| 构建（agents） | `npm run build:agents` | 退出码 0 |
| 构建（全量） | `npm run build` | 退出码 0 |
| 类型检查 | `npm run build:ts`（tsc） | 退出码 0 |

## 6. 性能与边界验证
> 无性能指标；边界以 EC 场景为主（EC-004 见 V3 V-08，EC-007 篇幅上限见 V2/V3 结构校验）

| 边界 | 要求 | 验证 |
|------|------|:--:|
| EC-004 非模板结构 | 不修改文件 + 提示三选项 | V3 V-08 |
| EC-007 篇幅超限 | 提示收敛/外移，不静默截断 | V2（≤400/≤200）+ V3 C3 |
| EC-006 章节不适用 | 保留标题骨架 | V2（H2 集合固定） |
| EC-009 空目录 | 从零规划并标注「初步规划」 | V5（锚点） |

## 7. 漂移检测

| 漂移类型 | 检测方法 |
|---------|---------|
| 孤立代码（有代码无需求） | `git status` 变更集 vs plan §5.1 变更清单（V4） |
| 需求缺失（有需求无实现） | FR/NFR/EC 逐项 grep 锚点（V5） |
| 规格漂移 | 复核 spec.md / SUP-001 与 agent 模板实际表述一致性（V5） |
| 文档基线漂移 | 核对 spec/plan 声明的「28 个既有输出模板」与实际计数（review C26） |

## 8. 结论
> 验证结论判定标准

**判定规则**：
- ✅ 通过：全部 5 组场景通过，0 阻塞，FR/NFR/EC 覆盖 100%
- ⚠️ 有条件通过：核心场景通过，存在低优先非阻塞改进项
- ❌ 不通过：存在阻塞问题或核心 FR（P0）未通过

## 9. 修订记录
> 记录本文档的版本变更历史

| 版本 | 变更说明 | 日期 | 修订人 |
|------|---------|------|--------|
| v1.0 | 初始创建 — 定义 V1~V5 五组场景（21 检查项），重点设计 V3 增量合并动态验证以补齐 review C20 交接 | 2026-09-22 | SDDU Validate Agent |
