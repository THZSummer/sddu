# 审查报告：specs-tree-roadmap-output-template

> **文档定位**: SDDU 审查策略 — 定义本 Feature 的 C1~C26 审查清单与判定标准；审查结果见 review-report.md
> **前置依赖**: spec.md（12 FR / 8 NFR / 10 EC + SUP-001）、plan.md（技术方案 + 3 ADR）、build.md（构建产物）
> **创建人**: SDDU Review Agent
> **创建时间**: 2026-09-22
> **版本**: v1.0
> **更新人**: SDDU Review Agent
> **更新时间**: 2026-09-22
> **更新说明**: 初始创建 — 基于 spec v1.0（含 SUP-001）+ plan v1.0 + 3 ADR + build.md，自主定义 C1~C26 审查清单（四维度）

## 1. 审查概要
> 审查结果的量化总览（此表为策略定义时的预估基线，最终数值以 review-report.md 为准）

| 维度 | 数值 |
|------|:--:|
| 审查文件数 | 3 个（1 NEW 输出模板 + 1 MODIFY agent 模板 + build.md 记录） |
| 规范基准 | 12 FR / 8 NFR / 10 EC / SUP-001 |
| 通过项 | — |
| 改进建议 | — |
| 阻塞问题 | — |

## 2. 自主审查清单（C1~C26）
> 审查 Agent 根据 spec/plan/build 产物自主定义具体审查项。

**审查对象来源**：
- `spec.md`：FR-001~012 / NFR-001~008 / EC-001~010 + SUP-001 → 逐项核验实现完整性
- `plan.md`：§4.1（标记语法 + 合并算法）+ §5.1（变更清单）+ ADR-001/002/003 → 架构遵循性检查
- `build.md`：§1 任务状态 + §2 变更清单 + §3 验证证据 → 覆盖完整性与证据可信度检查
- `src/templates/outputs/sddu-roadmap.md.hbs`（NEW）+ `src/templates/agents/sddu-roadmap.md.hbs`（MODIFY）→ 模板/指令质量检查
- README §209-211 工程约束 → 实现目标边界检查

**四维度指引**：
1. **规范符合性** — 对照 spec.md 逐 FR/NFR/EC/SUP-001 核验
2. **架构一致性** — 对照 plan.md ADR 与文件影响分析
3. **模板质量** — 模板可读性、标记自洽性、产物隔离、可解析性
4. **工程边界** — 变更集纳入范围与禁区（`.opencode/`、`.sddu/`、`scripts/`）

| # | 审查对象 | 审查基准 | 维度 | 审查方法 |
|---|---------|---------|------|---------|
| C1 | 新模板 `src/templates/outputs/sddu-roadmap.md.hbs` 单文件存在、含 `<<变量>>` 占位符、无渲染期逻辑 | FR-001 | 规范符合性 | 文件走查 + `grep -c '{{'` / 占位符计数 + 同目录附属文件排查 |
| C2 | 模板自描述元数据头（`文档定位` + `输出文件名: .sddu/ROADMAP.md`） | FR-002 | 规范符合性 | 头部逐行核对（模板 L4-L5） |
| C3 | 模板 **8** 个 H2 集合与顺序 == spec FR-003 表 | FR-003 | 规范符合性 | `grep '^## '` 提取与 spec 表逐行对照 |
| C4 | **9** 个 zone 与「固化 **6** + 保留 3」划分一致；保留区有显式标注 | FR-003 / FR-005 | 规范符合性 | `sddu:zone` 计数 + `mode="rewrite|preserve"` 分派核对 |
| C5 | 元数据头 = 统一 8 字段 + 3 专属字段（追加形式）；`前置依赖` 取固定值 | FR-004 | 规范符合性 | 头部字段逐一核对 + 固定值字符串比对 |
| C6 | 模板显式区分 rewrite/preserve，且能承载 plan 的合并机制 | FR-005 | 规范符合性 | 模板 zone 走查 + 对照 plan §4.1 合并算法 |
| C7 | 精简边界：H2 ≤ 8、篇幅指导上限 ≤ 400 行、内容准入；模板不含审计/覆盖率/附录章节 | FR-006 | 规范符合性 | H2 计数 + `grep` 排除章节关键字 |
| C8 | agent 模板 §6 为统一两级查找（用户自定义优先 > 插件内置兜底），与主流程 Agent 一致 | FR-007 | 规范符合性 | 与 `sddu-plan.md.hbs` §6 逐行结构比对 + 禁用语计数 |
| C9 | agent 模板 §5 内联格式骨架已删除，无第二处格式定义 | FR-008 | 规范符合性 | `grep '## 执行摘要 (前 20%)'` / `📊 输出格式` 计数 |
| C10 | agent 模板 §1 四字段自洽，`不输出版本路线图` 已修正为「不输出目录树」 | FR-009 | 规范符合性 | §1 走查 + 关键短语计数 |
| C11 | agent 模板含通用模板化判定标准（固定落盘产物即须模板化；零产物可豁免） | FR-010 | 规范符合性 | §7 规则 8 走查 + 与 SUP-001 语义并存核对 |
| C12 | 构建分发链路：dist 与 src 逐字节一致；`scripts/` 无需改动 | FR-011 | 规范符合性 | `diff src dist` + `git diff --stat -- scripts '*.cjs' '*.ts' package.json` |
| C13 | 旧/非模板结构 ROADMAP 非破坏性处理；无「自动迁移/自动重写」 | FR-012 / EC-004 | 规范符合性 | §5.4.2 Step A3 + §8 异常表走查 |
| C14 | SUP-001 生效：全文不再声明「内置固定格式 / 不通过外部模板文件定义 / 不走覆盖路径」 | SUP-001 | 规范符合性 | 禁用语全文件计数 + 两级路径与主流程 Agent 一致 |
| C15 | 风格与可维护性：元数据头/占位符/表格/缩进对齐既有模板；无 Tab；无 JS/`<script>` | NFR-001 / NFR-007 | 规范符合性 | 与 `sddu-plan.md.hbs`、`sddu-docs-*.md.hbs` 头对齐比对 + `grep -Pc '\t'` |
| C16 | 构建幂等 + 可覆盖性 + 失败可见 | NFR-002 / NFR-004 / NFR-005 | 规范符合性 | 二次构建 dist 哈希 + §6 优先级语义 + §8 EC-001/002 提示文案 |
| C17 | 可解析性 / 向后兼容 / 边界情况处置 | NFR-006 / NFR-008 / EC-001~010 | 规范符合性 | 标题唯一产出核验 + 其他模板零改动 + EC 逐项溯源 |
| C18 | 变更清单 == plan §5.1（NEW 1 + MODIFY 1，`scripts/` 不变） | plan §5.1 | 架构一致性 | `git status --porcelain src/` + `git diff --stat` |
| C19 | ADR-001/002/003 落地一致（标记语法 / 合并算法 / 零运行时代码） | ADR-001/002/003 | 架构一致性 | 模板 zone 走查 + agent §5.4 Step A–D 走查 |
| C20 | 增量合并机制验证充分性（幂等 / 保留 / 非模板结构三场景） | plan §8 / build §4 | 架构一致性 | build.md 证据链走查（识别静态 vs 动态验证缺口） |
| C21 | 标记语法自洽（zone/entry 配对、命名规范、表格例外、缺省安全语义） | plan §4.1 | 模板质量 | 模板与 §5.4.1 规则逐条对照 |
| C22 | 编写期说明与产物隔离（非 `sddu:` 注释不得入产物） | plan §4.1 | 模板质量 | 模板尾部 NOTE 走查 + 命名空间规则核对 |
| C23 | zone 语义描述与实际布局一致（meta zone 位于 H2 标题之上） | plan §4.2 | 模板质量 | 模板结构走查 + §5.4.1「标记只标注标题之下内容区」表述比对 |
| C24 | 工程边界：变更集 ⊆ 2 个 `src/` 文件（NG-005 / NFR-003） | NFR-003 / NG-005 | 工程边界 | `git status --porcelain` 全量核对 |
| C25 | 未触碰 `.opencode/`、`.sddu/ROADMAP.md`、旧 Feature `spec.md` | NG-004 / NG-005 | 工程边界 | `git status` 核对 + 禁区路径确认 |
| C26 | 流程文档基线一致性（spec/plan 声称的既有输出模板数与实际相符） | NFR-008 / NG-008 | 工程边界 | `find src/templates/outputs -name '*.hbs'` 计数 vs 文档声明 |

> **质量门槛（数量基线法）**：12 FR + 8 NFR + 10 EC + SUP-001 全部落到 C1~C17（规范符合性，17 条）；plan/ADR 架构检查 C18~C20（3 条）；模板质量 C21~C23（3 条）；工程边界 C24~C26（3 条）。四维度各 ≥1 条，Cx 总数 26 条，满足门槛。

> **基线复判更新（R3 / R4 / R5）**：C3（H2 集合）与 C4（zone 划分）的结构计数在 R3 新增「特性索引」章节后由 **7 个 H2 / 8 个 zone（5+3）** 变更为 **8 个 H2 / 9 个 zone（6+3）**；本表 C3 / C4 行的计数已按当前基线更正（判据文字与审查方法不变，仅更正计数）。相应定向重审由 G7 复判维持「通过」（见 `review-report.md` §9 / §11）；C7（H2 ≤ 8）同步改按 8 ≤ 8 判定、C21（zone 配对）改按 9 zone 判定。

## 3. 审查方法
> 静态审查（read-only）：文件走查 + `grep`/`diff`/`git status` 取证；不修改任何实现文件，不执行 `git commit`/`push`，不执行破坏性构建。

## 4. 结论判据（依据 sddu-review Agent §6 审查标准）

| 条件 | 要求 | 结论映射 |
|------|------|---------|
| 阻塞问题 | 0 个 | 否则 ❌ 不通过 |
| 改进项 | < 5 个 | ≥5 且非阻塞 → ⚠️ 有条件通过 |
| 规范符合率 | 100% | 否则 ⚠️ 有条件通过 |

## 5. 审查详情
> 逐项执行结果、维度汇总、阻塞/改进清单与结论见 `review-report.md`（按 ADR-004 策略与报告分离）
